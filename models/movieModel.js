const mongoose = require("mongoose");

const movieSchema = new mongoose.Schema({
    id: {
        type: 
    },
  title: {
    type: String,
    require: true,
  },
  year: {
    type: Number,
    required: true,
  },
  duration: {
    type: 
  },
  released: {

  }
});
